'use strict';

/*
let x = 0;
let y = 0;
let isDown = false;

[...$$('.chunk')].forEach(chunk => {
    chunk.addEventListener('drag', (e) => {
        e.preventDefault();
    });
})

map.addEventListener('mousedown', (ev) => {
    isDown = true;
    console.log("DOWN \n",ev)
}, true);

map.addEventListener('mouseup', (ev) => {
    isDown = false;
    console.log("UP \n", ev)
}, true);

*/
